<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="si.uni_lj.fe.tnuv.covidvremenar">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_ukrepi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".ProsnjaZaLokacijoActivity"></activity>
        <activity android:name=".UkrepiActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".SlovenijaActivity" />
        <activity android:name=".MojaObcinaActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>